import SiteTitle from "../../components/SiteTitle";

const Home = () => {
    return (
        <div>
            <SiteTitle>Home</SiteTitle>
            <h2>Home</h2>
        </div>
    );
};

export default Home;