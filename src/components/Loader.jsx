const Loader = () => {
    return (
        <div className='min-h-screen flex justify-center items-center'>
            <span className='loading loading-bars bg-primary'></span>
        </div>
    );
};

export default Loader;